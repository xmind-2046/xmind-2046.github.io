<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>xmind-2046</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://web-xmind.204680.xyz/</id>
  <link href="https://web-xmind.204680.xyz/" rel="alternate"/>
  <link href="https://web-xmind.204680.xyz/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, xmind-2046</rights>
  <subtitle>探索 · 记录 · 分享</subtitle>
  <title>xmind-2046</title>
  <updated>2026-08-28T13:25:34.202Z</updated>
  <entry>
    <author>
      <name>xmind-2046</name>
    </author>
    <category term="教程" scheme="https://web-xmind.204680.xyz/categories/%E6%95%99%E7%A8%8B/"/>
    <category term="代理" scheme="https://web-xmind.204680.xyz/tags/%E4%BB%A3%E7%90%86/"/>
    <category term="教程" scheme="https://web-xmind.204680.xyz/tags/%E6%95%99%E7%A8%8B/"/>
    <category term="v2rayN" scheme="https://web-xmind.204680.xyz/tags/v2rayN/"/>
    <category term="DNS" scheme="https://web-xmind.204680.xyz/tags/DNS/"/>
    <category term="网络优化" scheme="https://web-xmind.204680.xyz/tags/%E7%BD%91%E7%BB%9C%E4%BC%98%E5%8C%96/"/>
    <content>
      <![CDATA[<h1 id="v2rayN-完整优化指南：重置-·-分流-·-DNS-·-参数-全攻略"><a href="#v2rayN-完整优化指南：重置-·-分流-·-DNS-·-参数-全攻略" class="headerlink" title="v2rayN 完整优化指南：重置 · 分流 · DNS · 参数 全攻略"></a>v2rayN 完整优化指南：重置 · 分流 · DNS · 参数 全攻略</h1><blockquote><p>面向 <strong>v2rayN 7.x（Windows）</strong> 的一站式优化方案：从「恢复出厂设置」到「最优路由分流 &#x2F; DNS 防污染 &#x2F; 参数调优 &#x2F; TUN 模式」，覆盖 Xray 与 sing-box 双核心。<br>适用场景：配置越用越乱、分流失灵、想彻底重来一套干净的最优配置。所有设置均基于 v2rayN 官方讨论与社区实测方案整理。</p></blockquote><img src="/img/v2rayn-optimization-cover.svg" alt="v2rayN 路由分流与优化教程概览" loading="lazy" decoding="async" width="1600" height="960"><p><span style="color:#d14;font-weight:700">红色加粗内容 &#x3D; 需要注意或容易踩坑的地方，操作前务必阅读。</span><br><span style="color:#16803c;font-weight:700">绿色内容 &#x3D; 推荐做法或验证结果，可直接对照检查。</span></p><hr><h2 id="📌-开始之前：备份与版本检查"><a href="#📌-开始之前：备份与版本检查" class="headerlink" title="📌 开始之前：备份与版本检查"></a>📌 开始之前：备份与版本检查</h2><h3 id="1-1-检查版本"><a href="#1-1-检查版本" class="headerlink" title="1.1 检查版本"></a>1.1 检查版本</h3><p>打开 v2rayN 主界面，点击 <strong>帮助 → 检查更新</strong>，确保使用 <strong>7.x 最新稳定版</strong>（GitHub Releases 页面会显示最新版本号）。</p><blockquote><p>💡 <strong>7.x 才开始支持「指定特定网站走特定节点」（v7.17+）</strong>，旧版（6.x 及以下）路由界面与规则能力都有差距，建议先升级。</p></blockquote><h3 id="1-2-备份配置（重要！）"><a href="#1-2-备份配置（重要！）" class="headerlink" title="1.2 备份配置（重要！）"></a>1.2 备份配置（重要！）</h3><p>v2rayN 的所有配置集中在程序目录的 <code>guiConfigs</code> 文件夹，核心文件是 <code>guiNConfig.json</code>，其中包含：</p><ul><li>✅ 全部订阅地址</li><li>✅ 全部节点（含密钥信息）</li><li>✅ 自定义路由规则</li><li>✅ 参数设置</li></ul><blockquote><p>🔴 <strong>安全提醒</strong>：配置文件包含所有节点的密钥，等同账号密码。备份到网盘时务必<strong>加密压缩</strong>（如 7-Zip 带密码），不要明文上传到公开位置。</p></blockquote><p><span style="color:#16803c;font-weight:700">推荐做法：直接复制整个 v2rayN 文件夹（连内核、规则文件一起备份），恢复到新电脑时覆盖解压即可。</span></p><hr><h2 id="①-恢复出厂设置（重置）"><a href="#①-恢复出厂设置（重置）" class="headerlink" title="① 恢复出厂设置（重置）"></a>① 恢复出厂设置（重置）</h2><p>v2rayN <strong>没有内置</strong>「恢复出厂」按钮。重置的本质是 <strong>让程序重新生成配置目录</strong>，所以整段流程围绕「退出 → 改目录名 → 重启」展开。</p><h3 id="步骤-1：备份订阅链接"><a href="#步骤-1：备份订阅链接" class="headerlink" title="步骤 1：备份订阅链接"></a>步骤 1：备份订阅链接</h3><p>订阅和节点都存在数据库里，重置后会<strong>全部丢失</strong>。打开：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">v2rayN → 订阅分组 → 双击订阅组 → 复制「地址(url)」</span><br></pre></td></tr></table></figure><p>把所有订阅 URL 存到记事本备用。</p><h3 id="步骤-2：完全退出-v2rayN"><a href="#步骤-2：完全退出-v2rayN" class="headerlink" title="步骤 2：完全退出 v2rayN"></a>步骤 2：完全退出 v2rayN</h3><p>托盘图标右键 → <strong>退出</strong>。</p><blockquote><p>🔴 <strong>必须在退出状态下操作文件</strong>，否则配置文件被进程锁定，会报 <code>Access denied</code>。</p></blockquote><h3 id="步骤-3：重命名配置目录（不删除-等于备份）"><a href="#步骤-3：重命名配置目录（不删除-等于备份）" class="headerlink" title="步骤 3：重命名配置目录（不删除 &#x3D; 等于备份）"></a>步骤 3：重命名配置目录（不删除 &#x3D; 等于备份）</h3><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> D:\你的v2rayN目录</span><br><span class="line"><span class="built_in">Rename-Item</span> guiConfigs guiConfigs.bak</span><br><span class="line"><span class="built_in">Rename-Item</span> binConfigs binConfigs.bak   <span class="comment"># 不存在也没关系，它只在核心运行时生成</span></span><br></pre></td></tr></table></figure><h3 id="步骤-4：重新启动-v2rayN"><a href="#步骤-4：重新启动-v2rayN" class="headerlink" title="步骤 4：重新启动 v2rayN"></a>步骤 4：重新启动 v2rayN</h3><p>程序会自动生成全新默认配置，然后重新添加订阅：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">订阅分组 → 添加 → 粘贴订阅 URL → 更新订阅（Ctrl+U）</span><br></pre></td></tr></table></figure><blockquote><p>💡 若更新订阅失败（订阅域名被墙），勾选「<strong>更新订阅时使用代理</strong>」，或先手动导入一个节点再更新。</p></blockquote><h3 id="步骤-5：清理垃圾文件"><a href="#步骤-5：清理垃圾文件" class="headerlink" title="步骤 5：清理垃圾文件"></a>步骤 5：清理垃圾文件</h3><table><thead><tr><th align="left">文件</th><th align="left">处理</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left"><code>AmazTool.exe.tmp</code></td><td align="left">🗑️ 可删</td><td align="left">升级残留</td></tr><tr><td align="left"><code>AmazTool.exe</code></td><td align="left">✅ 保留</td><td align="left">更新工具本体</td></tr><tr><td align="left"><code>guiConfigs.bak</code> &#x2F; <code>*.bak</code></td><td align="left">⏳ 缓删</td><td align="left">确认新配置稳定<strong>几天后</strong>再删</td></tr></tbody></table><blockquote><p>💡 <strong>迁移到新电脑</strong>：把备份的 <code>guiConfigs</code>（或整个文件夹）覆盖到新解压的 v2rayN 目录，启动后更新一次订阅即可。</p></blockquote><hr><h2 id="②-路由分流设置（核心）"><a href="#②-路由分流设置（核心）" class="headerlink" title="② 路由分流设置（核心）"></a>② 路由分流设置（核心）</h2><p><strong>位置：设置 → 路由设置</strong></p><h3 id="2-1-规则集预设"><a href="#2-1-规则集预设" class="headerlink" title="2.1 规则集预设"></a>2.1 规则集预设</h3><table><thead><tr><th align="left">设置项</th><th align="left">推荐值</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left">规则集预设</td><td align="left"><strong>绕过局域网(LAN)和大陆(Chn)</strong>（白名单模式）</td><td align="left">国内直连 + 国外代理，最均衡的方案</td></tr><tr><td align="left">域名解析策略</td><td align="left"><strong>IPIfNonMatch</strong></td><td align="left">见 2.2</td></tr><tr><td align="left">启用嗅探 (Sniffing)</td><td align="left">✅ 开启，DestOverride 勾选 <code>http</code> + <code>tls</code> + <code>quic</code></td><td align="left"><strong>quic 很关键</strong>：兜住浏览器 ECH&#x2F;HTTP3 流量，否则分流会失灵</td></tr><tr><td align="left">RouteOnly</td><td align="left">❌ 关闭</td><td align="left">开了只分流不转发，日常不要开</td></tr></tbody></table><h3 id="2-2-域名解析策略详解"><a href="#2-2-域名解析策略详解" class="headerlink" title="2.2 域名解析策略详解"></a>2.2 域名解析策略详解</h3><p>Xray 内核提供三种策略，<strong>理解它们是分流准确的关键</strong>：</p><table><thead><tr><th align="left">策略</th><th align="left">行为</th><th align="left">适用</th></tr></thead><tbody><tr><td align="left"><code>AsIs</code></td><td align="left">域名未命中路由表时，<strong>不解析 IP</strong>，直接走代理</td><td align="left">追求极速、几乎不访问国内站</td></tr><tr><td align="left"><code>IPIfNonMatch</code></td><td align="left">域名不在 <code>geosite:cn</code> 名单时，<strong>先解析成 IP</strong> 再对比 <code>geoip:cn</code></td><td align="left">⭐ <strong>推荐</strong>：兜住漏网的大陆网站</td></tr><tr><td align="left"><code>IPOnDemand</code></td><td align="left">只要命中任何 IP 规则就立即解析</td><td align="left">与 IPIfNonMatch 类似，更激进</td></tr></tbody></table><blockquote><p>📌 sing-box 内核对应项为「IPv4&#x2F;IPv6 优先」系列，默认同为 <code>AsIs</code> 模式；选择「IPv4 优先」可视为 IPIf 的等价物。</p></blockquote><p><strong>白名单 vs 黑名单怎么选？</strong></p><ul><li>🟢 <strong>白名单（绕过大陆）</strong>：只有命中「大陆&#x2F;局域网」规则的才直连，其余全走代理 → <span style="color:#16803c;font-weight:700">隐私和体验最好，日常推荐</span></li><li>🔵 <strong>黑名单（绕过代理侧）</strong>：只有命中「被墙名单」的才走代理 → 更省流量但<strong>漏网多</strong></li></ul><h3 id="2-3-可选进阶规则"><a href="#2-3-可选进阶规则" class="headerlink" title="2.3 可选进阶规则"></a>2.3 可选进阶规则</h3><p>在路由设置里手动添加：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">geosite:category-ads-all  →  拦截    （去广告）</span><br><span class="line">geosite:cn,geoip:private,cn  →  直连  （预设已含，按需补）</span><br></pre></td></tr></table></figure><h3 id="2-4-规则写法速查"><a href="#2-4-规则写法速查" class="headerlink" title="2.4 规则写法速查"></a>2.4 规则写法速查</h3><table><thead><tr><th align="left">写法</th><th align="left">说明</th><th align="left">示例</th></tr></thead><tbody><tr><td align="left"><code>domain:完整域名</code></td><td align="left">匹配该域名及其子域名</td><td align="left"><code>domain:www.google.com</code></td></tr><tr><td align="left"><code>keyword:关键字</code></td><td align="left">包含关键字即匹配</td><td align="left"><code>keyword:google</code></td></tr><tr><td align="left"><code>regexp:正则</code></td><td align="left">高级复杂匹配</td><td align="left"><code>regexp:.*\.googlevideo\.com</code></td></tr><tr><td align="left"><code>full:完整字符串</code></td><td align="left">完全一致才匹配</td><td align="left"><code>full:example.com</code></td></tr><tr><td align="left"><code>geosite:域名集</code></td><td align="left">引用预定义列表</td><td align="left"><code>geosite:cn</code></td></tr><tr><td align="left"><code>geoip:IP集</code></td><td align="left">引用预定义 IP 列表</td><td align="left"><code>geoip:cn</code></td></tr><tr><td align="left"><code>IP</code> &#x2F; <code>IP段(CIDR)</code></td><td align="left">精确&#x2F;网段匹配</td><td align="left"><code>8.8.8.8</code> &#x2F; <code>192.168.1.0/24</code></td></tr></tbody></table><h3 id="2-5-进阶：指定网站走指定节点（v7-17-）"><a href="#2-5-进阶：指定网站走指定节点（v7-17-）" class="headerlink" title="2.5 进阶：指定网站走指定节点（v7.17+）"></a>2.5 进阶：指定网站走指定节点（v7.17+）</h3><p><strong>示例</strong>：<code>OpenAI</code> 走美国节点、<code>Netflix</code> 走新加坡节点、游戏走专线。</p><ol><li>进入 <strong>设置 → 路由设置 → 高级功能 → 添加规则</strong></li><li>配置规则：<ul><li><strong>别名</strong>：如 <code>Special_OpenAI</code></li><li><strong>outboundTag</strong>：⚠️ 不要手动输入 <code>proxy</code>，点击右侧 <strong>「选择配置」</strong> 按钮选择目标节点（自动填入节点 ID&#x2F;Tag）</li><li><strong>Domain</strong>：推荐 <code>keyword:xxx</code> 或 <code>domain:xxx</code> 写法</li></ul></li><li><strong>调整优先级</strong>：保存后将新规则<strong>上移</strong>。路由匹配<strong>从上到下</strong>，特定规则必须放在通用规则（如「绕过大陆」）<strong>上面</strong>，否则会被通用规则拦截</li></ol><p><span style="color:#d14;font-weight:700">容易踩坑：规则顺序错了最隐蔽——特定规则被上方通用规则拦截时，表现是「规则没生效」，但路由本身是正常的。</span></p><h3 id="2-6-完整路由方案（可直接参考）"><a href="#2-6-完整路由方案（可直接参考）" class="headerlink" title="2.6 完整路由方案（可直接参考）"></a>2.6 完整路由方案（可直接参考）</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">1. block 广告域名集 (geosite:category-ads-all)      → block    （去广告）</span><br><span class="line">2. 特殊网站规则（OpenAI/Netflix/游戏专线）            → 指定节点  （放最前！）</span><br><span class="line">3. proxy 常用被墙域名 (geosite:geolocation-!cn)      → proxy</span><br><span class="line">4. direct 中国域名 (geosite:cn)                      → direct</span><br><span class="line">5. direct 中国 IP (geoip:cn)                         → direct</span><br><span class="line">6. direct 局域网 IP (geoip:private)                  → direct</span><br><span class="line">7. 兜底规则（匹配所有剩余流量）                        → proxy</span><br></pre></td></tr></table></figure><h3 id="2-7-规则文件加强版（可选）"><a href="#2-7-规则文件加强版（可选）" class="headerlink" title="2.7 规则文件加强版（可选）"></a>2.7 规则文件加强版（可选）</h3><blockquote><p><a href="https://github.com/Loyalsoldier/v2ray-rules-dat">Loyalsoldier&#x2F;v2ray-rules-dat</a>：官方 <code>geoip.dat</code> &#x2F; <code>geosite.dat</code> 的 <strong>加强替代版</strong>，覆盖更全、更新更勤（每 12 小时自动构建），兼容 Xray &#x2F; sing-box &#x2F; mihomo &#x2F; hysteria 等。追求分流准确度可选用。</p></blockquote><hr><h2 id="③-DNS-设置（防污染-提速）"><a href="#③-DNS-设置（防污染-提速）" class="headerlink" title="③ DNS 设置（防污染 + 提速）"></a>③ DNS 设置（防污染 + 提速）</h2><p><strong>位置：设置 → DNS 设置（简化模式）</strong></p><table><thead><tr><th align="left">设置项</th><th align="left">推荐值</th><th align="left">原因</th></tr></thead><tbody><tr><td align="left">直连 DNS（国内）</td><td align="left"><code>https://dns.alidns.com/dns-query</code></td><td align="left">阿里 DoH，国内解析快且稳（可换腾讯 <code>https://doh.pub/dns-query</code>）</td></tr><tr><td align="left">远程 DNS（国外）</td><td align="left"><code>https://cloudflare-dns.com/dns-query</code></td><td align="left">走代理解析，无污染</td></tr><tr><td align="left">Bootstrap DNS</td><td align="left"><code>223.5.5.5</code></td><td align="left">用来解析上面两个 DoH 域名本身</td></tr><tr><td align="left">FakeIP</td><td align="left">❌ <strong>关闭</strong></td><td align="left">系统代理模式下没必要，开了部分软件会异常</td></tr><tr><td align="left">拦截 AAAA (IPv6)</td><td align="left">按需（无 IPv6 需求可开）</td><td align="left">—</td></tr><tr><td align="left">使用系统 Hosts</td><td align="left">✅ 开启</td><td align="left">让本机 hosts 文件生效</td></tr></tbody></table><blockquote><p>📌 这套「<strong>国内域名走阿里 DoH 直连解析、国外域名走 Cloudflare DoH 代理解析</strong>」的组合，是防 DNS 污染 + 保证解析速度的标准做法。</p></blockquote><h3 id="3-1-远程解析策略（重要）"><a href="#3-1-远程解析策略（重要）" class="headerlink" title="3.1 远程解析策略（重要）"></a>3.1 远程解析策略（重要）</h3><p>Xray 内核的「远程解析策略」建议设为 <strong><code>use ip</code></strong>（双栈）；若你的 VPS 为双栈但 IPv6 线路故障，会出现 <strong>访问 YouTube 首页卡 20 多秒</strong> 的典型症状——这是 VPS 默认先连 IPv6、失败后才回退 IPv4 导致的。</p><p><span style="color:#d14;font-weight:700">遇到上述卡顿，按下面对应内核修改：</span></p><ul><li><strong>Xray 内核</strong>：远程解析策略改为 <code>useipv4</code>（IPv4 优先）</li><li><strong>sing-box 内核</strong>：直连和远程 DNS 都选 <strong>「IPv4 优先」</strong></li></ul><p><span style="color:#16803c;font-weight:700">验证方法：改完再开 YouTube，首页视频应立即开始加载，不再有 20 秒等待。</span></p><h3 id="3-2-常见公共-DNS-参考"><a href="#3-2-常见公共-DNS-参考" class="headerlink" title="3.2 常见公共 DNS 参考"></a>3.2 常见公共 DNS 参考</h3><table><thead><tr><th align="left">服务商</th><th align="left">DoH 地址</th><th align="left">特点</th></tr></thead><tbody><tr><td align="left">阿里 DNS</td><td align="left"><code>https://dns.alidns.com/dns-query</code></td><td align="left">国内解析快、稳</td></tr><tr><td align="left">腾讯 DNSPod</td><td align="left"><code>https://doh.pub/dns-query</code></td><td align="left">国内备用</td></tr><tr><td align="left">Cloudflare</td><td align="left"><code>https://cloudflare-dns.com/dns-query</code></td><td align="left">国外、无污染</td></tr><tr><td align="left">Google</td><td align="left"><code>https://dns.google/dns-query</code></td><td align="left">国外备用</td></tr></tbody></table><hr><h2 id="④-参数设置（性能与体验）"><a href="#④-参数设置（性能与体验）" class="headerlink" title="④ 参数设置（性能与体验）"></a>④ 参数设置（性能与体验）</h2><p><strong>位置：设置 → 参数设置</strong></p><table><thead><tr><th align="left">设置项</th><th align="left">推荐值</th><th align="left">原因</th></tr></thead><tbody><tr><td align="left">Hysteria2 上&#x2F;下行带宽</td><td align="left">填<strong>真实宽带值</strong>（如 300&#x2F;50 Mbps）</td><td align="left">虚高会丢包重传，反而变慢</td></tr><tr><td align="left">Fragment 分片</td><td align="left">❌ 关闭</td><td align="left">只在节点被 SNI 阻断时当救命稻草，平时拖慢握手</td></tr><tr><td align="left">Mux 多路复用</td><td align="left">❌ 保持关闭</td><td align="left">流媒体&#x2F;下载开 Mux 反而降速</td></tr><tr><td align="left">订阅自动更新间隔</td><td align="left">20 → <strong>60</strong> 分钟</td><td align="left">够用，减少无谓请求</td></tr><tr><td align="left">测速方式</td><td align="left"><strong>「真连接延迟」</strong></td><td align="left">TCP ping 不代表实际体验</td></tr><tr><td align="left">启用流量统计 &#x2F; 实时网速</td><td align="left">✅ 开启</td><td align="left">便于观察分流效果</td></tr><tr><td align="left">自动运行（开机自启）</td><td align="left">按需</td><td align="left">—</td></tr><tr><td align="left">TUN 模式</td><td align="left">❌ 保持关闭</td><td align="left">系统代理日常够用，详见第 ⑤ 节</td></tr></tbody></table><blockquote><p>🔴 <strong>Hysteria2 带宽填虚高的后果</strong>：部分用户以为「填得越大跑得越快」，实际上会造成丢包重传、连接不稳，<strong>数字越大越慢</strong>。真实宽带的「下行&#x2F;上行」分别填到对应栏即可。</p></blockquote><hr><h2 id="⑤-TUN-模式（进阶）"><a href="#⑤-TUN-模式（进阶）" class="headerlink" title="⑤ TUN 模式（进阶）"></a>⑤ TUN 模式（进阶）</h2><h3 id="何时需要-TUN？"><a href="#何时需要-TUN？" class="headerlink" title="何时需要 TUN？"></a>何时需要 TUN？</h3><p>系统代理只对 <strong>认代理设置</strong> 的程序生效。遇到以下情况才需要 TUN 模式：</p><ul><li>🎮 游戏 &#x2F; UWP 应用 &#x2F; 命令行工具不走系统代理</li><li>📡 需要全局接管所有流量</li></ul><h3 id="开启要点"><a href="#开启要点" class="headerlink" title="开启要点"></a>开启要点</h3><table><thead><tr><th align="left">项目</th><th align="left">推荐</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left">前置条件</td><td align="left"><strong>以管理员身份运行</strong> v2rayN</td><td align="left">否则虚拟网卡创建失败</td></tr><tr><td align="left">Stack</td><td align="left"><code>gvisor</code>（兼容最好）或 <code>mixed</code></td><td align="left">—</td></tr><tr><td align="left">Strict Route</td><td align="left">✅ 开</td><td align="left">防流量泄漏</td></tr><tr><td align="left">MTU</td><td align="left">9000</td><td align="left">—</td></tr><tr><td align="left">启用缓存文件 (EnableCacheFile4Sbox)</td><td align="left">✅ 勾选</td><td align="left">sing-box 记住 UDP 连接，游戏&#x2F;NTP 更稳</td></tr><tr><td align="left">系统代理</td><td align="left">❌ 关闭</td><td align="left">开了 TUN 就别同时开系统代理，避免冲突</td></tr></tbody></table><blockquote><p>💡 TUN 模式接管的是<strong>系统层面</strong>的流量，路由规则依然生效。若需按进程分流，在规则的 <code>process</code> 栏填写进程名（如 <code>chrome.exe</code>）。</p></blockquote><h3 id="TUN-vs-系统代理"><a href="#TUN-vs-系统代理" class="headerlink" title="TUN vs 系统代理"></a>TUN vs 系统代理</h3><table><thead><tr><th align="left">对比项</th><th align="left">系统代理</th><th align="left">TUN 模式</th></tr></thead><tbody><tr><td align="left">覆盖范围</td><td align="left">仅认代理的程序</td><td align="left">所有流量</td></tr><tr><td align="left">适用场景</td><td align="left">浏览器为主</td><td align="left">游戏、UWP、命令行</td></tr><tr><td align="left">资源占用</td><td align="left">低</td><td align="left">稍高</td></tr><tr><td align="left">日常推荐</td><td align="left">⭐ 推荐</td><td align="left">按需开启</td></tr></tbody></table><hr><h2 id="⑥-Geo-数据库与内核更新"><a href="#⑥-Geo-数据库与内核更新" class="headerlink" title="⑥ Geo 数据库与内核更新"></a>⑥ Geo 数据库与内核更新</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">主界面 → 检查更新 → 勾选 GeoFiles → 下载并安装</span><br></pre></td></tr></table></figure><ul><li><code>geoip</code> &#x2F; <code>geosite</code> 数据决定分流准确度，<strong>每月手动更新一次</strong>。</li><li>核心类型说明：VMess&#x2F;VLESS&#x2F;Trojan 走 <strong>Xray 核心</strong>，Hysteria2&#x2F;TUIC&#x2F;AnyTLS 走 <strong>sing-box</strong>（自动判断即可，一般不用手动指定）。</li><li>追求更全规则可替换为 Loyalsoldier 加强版（见 <a href="#27-%E8%A7%84%E5%88%99%E6%96%87%E4%BB%B6%E5%8A%A0%E5%BC%BA%E7%89%88%E5%8F%AF%E9%80%89">2.7</a>）。</li></ul><hr><h2 id="⑦-验证与排错"><a href="#⑦-验证与排错" class="headerlink" title="⑦ 验证与排错"></a>⑦ 验证与排错</h2><h3 id="7-1-验证分流是否生效"><a href="#7-1-验证分流是否生效" class="headerlink" title="7.1 验证分流是否生效"></a>7.1 验证分流是否生效</h3><ol><li>🌍 浏览器开<strong>无痕窗口</strong>，访问 <code>https://ip.sb</code> → 应显示<strong>节点机房 IP</strong>（国外流量走代理 ✅）</li><li>🇨🇳 访问 <code>https://www.ip.cn</code> → 应显示<strong>你家本地 IP</strong>（国内流量直连 ✅）</li><li>📊 看 v2rayN 主界面「今日上传&#x2F;下载」列：刷国内站时代理流量基本不涨 → 分流正常</li></ol><h3 id="7-2-常见问题排查"><a href="#7-2-常见问题排查" class="headerlink" title="7.2 常见问题排查"></a>7.2 常见问题排查</h3><table><thead><tr><th align="left">现象</th><th align="left">可能原因</th><th align="left">解决</th></tr></thead><tbody><tr><td align="left">YouTube 首页卡 20+ 秒</td><td align="left">VPS 双栈但 IPv6 故障，先连 IPv6 失败才回退</td><td align="left">远程解析策略改 <code>useipv4</code> &#x2F; 「IPv4 优先」</td></tr><tr><td align="left">国内网站走了代理变慢</td><td align="left">域名解析策略不对</td><td align="left">改用 <code>IPIfNonMatch</code></td></tr><tr><td align="left">部分网页打不开</td><td align="left">嗅探没开 quic</td><td align="left">DestOverride 勾选 <code>http + tls + quic</code></td></tr><tr><td align="left">更新订阅失败</td><td align="left">订阅域名被墙</td><td align="left">勾选「更新订阅时使用代理」</td></tr><tr><td align="left">重命名配置目录报 <code>Access denied</code></td><td align="left">v2rayN 还在运行</td><td align="left">先完全退出托盘程序</td></tr><tr><td align="left">TUN 开启失败</td><td align="left">非管理员权限 &#x2F; 被杀软拦截</td><td align="left">右键管理员运行，放行杀软</td></tr></tbody></table><hr><h2 id="⑧-日常维护清单"><a href="#⑧-日常维护清单" class="headerlink" title="⑧ 日常维护清单"></a>⑧ 日常维护清单</h2><table><thead><tr><th align="left">频率</th><th align="left">事项</th></tr></thead><tbody><tr><td align="left">📅 每月</td><td align="left">更新 GeoFiles（检查更新 → GeoFiles）</td></tr><tr><td align="left">🐢 节点变慢时</td><td align="left">全选节点 → 测试真连接延迟 → 换延迟最低的</td></tr><tr><td align="left">📭 订阅失效时</td><td align="left">更新订阅；还不行就是订阅后端挂了</td></tr><tr><td align="left">💾 免费 VPS（如 EUserv）</td><td align="left">每 3~4 周登录一次控制台保活</td></tr></tbody></table><hr><h2 id="⚡-TL-DR-速查"><a href="#⚡-TL-DR-速查" class="headerlink" title="⚡ TL;DR 速查"></a>⚡ TL;DR 速查</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────────┐</span><br><span class="line">│  路由：绕过大陆(白名单) + IPIfNonMatch + 嗅探含quic      │</span><br><span class="line">│  DNS ：直连阿里DoH / 远程Cloudflare DoH / Bootstrap 223.5.5.5 / FakeIP关 │</span><br><span class="line">│  参数：带宽填真实值、Fragment关、Mux关、订阅60分钟        │</span><br><span class="line">│  验证：ip.sb 显节点IP + ip.cn 显家宽IP = 分流正常        │</span><br><span class="line">│  维护：每月更 GeoFiles，测速用真连接延迟                  │</span><br><span class="line">│  排错：YouTube卡顿 → IPv4优先；网页打不开 → 开嗅探quic   │</span><br><span class="line">└──────────────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure><hr><h2 id="附：参考来源"><a href="#附：参考来源" class="headerlink" title="附：参考来源"></a>附：参考来源</h2><ul><li><a href="https://github.com/2dust/v2rayN">v2rayN 官方仓库（GitHub）</a></li><li><a href="https://github.com/2dust/v2rayN/discussions/8791">v2rayN DNS 和路由设置指南 · v2rayN Discussion #8791</a></li><li><a href="https://0xdadream.github.io/2026/02/01/v2rayn-lu-you-gui-ze/">v2rayN 路由规则详解与分流教程 · 逐梦</a></li><li><a href="https://v2ray-org.cn/zh-cn/blog/backup-migrate/">备份与迁移 v2rayN 配置 · v2ray-org.cn</a></li><li><a href="https://github.com/Loyalsoldier/v2ray-rules-dat">Loyalsoldier&#x2F;v2ray-rules-dat（GitHub）</a></li></ul><hr><p><em>本文基于 v2rayN 官方讨论区、社区分流教程与实测整理：白名单分流 + IPIfNonMatch + 嗅探 quic、国内外 DoH 组合、远程解析 IPv4 优先（YouTube 卡顿）、Hysteria2 真实带宽、TUN 模式要点等均为验证过的关键点。</em></p>]]>
    </content>
    <id>https://web-xmind.204680.xyz/2026/08/28/v2rayN-%E5%AE%8C%E6%95%B4%E4%BC%98%E5%8C%96%E6%8C%87%E5%8D%97/</id>
    <link href="https://web-xmind.204680.xyz/2026/08/28/v2rayN-%E5%AE%8C%E6%95%B4%E4%BC%98%E5%8C%96%E6%8C%87%E5%8D%97/"/>
    <published>2026-08-28T10:30:00.000Z</published>
    <summary>v2rayN 完整优化指南：恢复出厂设置（重置）、路由分流（白名单/IPIfNonMatch/嗅探quic）、DNS 防污染（阿里DoH + Cloudflare DoH）、远程解析 IPv4 优先、Hysteria2 带宽等参数调优、TUN 模式与常见故障排错，附 TL;DR 速查。</summary>
    <title>v2rayN 完整优化指南：重置 · 分流 · DNS · 参数 全攻略</title>
    <updated>2026-08-28T13:25:34.202Z</updated>
  </entry>
  <entry>
    <author>
      <name>xmind-2046</name>
    </author>
    <category term="教程" scheme="https://web-xmind.204680.xyz/categories/%E6%95%99%E7%A8%8B/"/>
    <category term="Hysteria2" scheme="https://web-xmind.204680.xyz/tags/Hysteria2/"/>
    <category term="VPS" scheme="https://web-xmind.204680.xyz/tags/VPS/"/>
    <category term="IPv6" scheme="https://web-xmind.204680.xyz/tags/IPv6/"/>
    <category term="代理" scheme="https://web-xmind.204680.xyz/tags/%E4%BB%A3%E7%90%86/"/>
    <category term="教程" scheme="https://web-xmind.204680.xyz/tags/%E6%95%99%E7%A8%8B/"/>
    <content>
      <![CDATA[<h1 id="纯-IPv6-VPS-搭建-Hysteria-2-节点：从零到稳定运行的完整教程"><a href="#纯-IPv6-VPS-搭建-Hysteria-2-节点：从零到稳定运行的完整教程" class="headerlink" title="纯 IPv6 VPS 搭建 Hysteria 2 节点：从零到稳定运行的完整教程"></a>纯 IPv6 VPS 搭建 Hysteria 2 节点：从零到稳定运行的完整教程</h1><blockquote><p>面向 <strong>纯 IPv6（IPv6-Only）VPS</strong> 的 Hysteria 2 搭建全流程：<strong>先装 Cloudflare WARP 打通 IPv4 出口</strong>，再安装 Hysteria 2、生成带 SAN 的自签证书、客户端固定证书零警告接入。<br>适用环境：<strong>Ubuntu 24.04 · 64bit</strong>，EUserv 等免费纯 IPv6 VPS 同样适用。文中所有命令均经真实部署验证。</p></blockquote><img src="/img/hysteria2-ipv6-cover.svg" alt="纯 IPv6 VPS 搭建 Hysteria 2 教程概览" loading="lazy" decoding="async" width="1600" height="960"><hr><h2 id="📌-开始之前：占位符说明"><a href="#📌-开始之前：占位符说明" class="headerlink" title="📌 开始之前：占位符说明"></a>📌 开始之前：占位符说明</h2><p>本文为保护隐私，统一使用以下<strong>示例值</strong>。复制命令前，请先建立一份自己的参数清单；凡是示例值都要替换，不能把下面的域名、IPv6 或密码直接用于生产环境。</p><p><span style="color:#d14;font-weight:700">红色加粗内容 &#x3D; 脱敏示例，必须按你的环境修改。</span><br><span style="color:#16803c;font-weight:700">绿色内容 &#x3D; 验证结果或安全建议，可直接对照检查。</span></p><table><thead><tr><th align="left">占位符</th><th align="left">含义</th><th align="left">示例</th></tr></thead><tbody><tr><td align="left"><span style="color:#d14;font-weight:700"><code>yourname.ddns.org</code></span></td><td align="left">解析到 VPS 的域名（DDNS 亦可）</td><td align="left"><span style="color:#d14"><code>yourname.ddns.org</code></span></td></tr><tr><td align="left"><span style="color:#d14;font-weight:700"><code>2001:db8:abcd::1</code></span></td><td align="left">VPS 的公网 IPv6 地址</td><td align="left"><span style="color:#d14"><code>2001:db8:abcd::1</code></span></td></tr><tr><td align="left"><span style="color:#d14;font-weight:700"><code>YourStrongPassword!2026</code></span></td><td align="left">Hysteria 2 认证密码</td><td align="left"><span style="color:#16803c">使用随机生成器生成 20 位以上密码</span></td></tr></tbody></table><h3 id="参数清单（建议先填好）"><a href="#参数清单（建议先填好）" class="headerlink" title="参数清单（建议先填好）"></a>参数清单（建议先填好）</h3><table><thead><tr><th align="left">参数</th><th align="left">你的实际值</th><th align="left">会出现在哪些地方</th></tr></thead><tbody><tr><td align="left">域名</td><td align="left"><span style="color:#d14;font-weight:700"><code>yourname.ddns.org</code></span></td><td align="left">DNS、证书 SAN、SNI、导入链接</td></tr><tr><td align="left">VPS IPv6</td><td align="left"><span style="color:#d14;font-weight:700"><code>2001:db8:abcd::1</code></span></td><td align="left">AAAA 记录、证书 IP SAN</td></tr><tr><td align="left">认证密码</td><td align="left"><span style="color:#d14;font-weight:700"><code>YourStrongPassword!2026</code></span></td><td align="left">服务端 <code>auth.password</code>、客户端密码</td></tr><tr><td align="left">端口</td><td align="left"><code>8443</code></td><td align="left">服务端监听、防火墙、客户端</td></tr></tbody></table><blockquote><p><strong>脱敏规则</strong>：发布截图、日志或配置时，域名、完整 IPv6、认证密码、证书 PEM 都应再次打码。尤其不要把真实 <code>server.key</code>、完整客户端链接或密码提交到公开仓库。</p></blockquote><p><strong>前置条件速查</strong>（任何一项不满足，后面都会以各种奇怪现象报错）：</p><ul><li>✅ 服务器：Ubuntu 24.04，有公网 IPv6，可用 root 或 sudo</li><li>✅ 域名：一个 <strong>AAAA 记录指向 VPS IPv6</strong> 的域名（不要用 IP 直连，Hysteria 2 依赖 SNI）；若托管在 Cloudflare 务必开 <strong>DNS Only（灰云）</strong>，橙云代理模式不支持 UDP</li><li>✅ 客户端：v2rayN 7.x + <strong>sing-box 核心</strong>（不要用 Xray 跑 Hysteria2，证书字段有 bug）</li><li>✅ 本机网络：<strong>必须支持 IPv6</strong>，先用下面命令自测：<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ping -6 -n 3 2606:4700:4700::1111   <span class="comment"># Cloudflare DNS</span></span><br><span class="line">ping -6 -n 3 2001:4860:4860::8888   <span class="comment"># Google DNS</span></span><br></pre></td></tr></table></figure></li></ul><h3 id="阅读与执行方式"><a href="#阅读与执行方式" class="headerlink" title="阅读与执行方式"></a>阅读与执行方式</h3><ol><li>命令默认以 <code>root</code> 执行；如果使用普通用户，请在需要时加 <code>sudo</code>，并确认该用户属于 sudo 组。</li><li>每完成一个阶段先执行对应的验证命令。验证失败时不要继续堆叠配置，先处理当前网络、解析或权限问题。</li><li>文中的 <code>8443</code> 只是示例端口。改端口时必须同时修改 Hysteria 配置、UFW、安全组和客户端，不能只改其中一处。</li></ol><h3 id="开始前备份与记录"><a href="#开始前备份与记录" class="headerlink" title="开始前备份与记录"></a>开始前备份与记录</h3><p>先记录当前网络和系统状态，便于故障时判断是安装前还是安装后出现问题：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> /etc/os-release</span><br><span class="line">ip -6 addr show scope global</span><br><span class="line">ip -6 route</span><br><span class="line"><span class="built_in">date</span> -u</span><br></pre></td></tr></table></figure><p>如果服务器上已经有 Hysteria 配置，先备份：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p /root/hysteria-backup</span><br><span class="line"><span class="built_in">cp</span> -a /etc/hysteria /root/hysteria-backup/config-$(<span class="built_in">date</span> +%F-%H%M%S)</span><br></pre></td></tr></table></figure><hr><h2 id="①-首先安装-Cloudflare-WARP（打通-IPv4-出口）"><a href="#①-首先安装-Cloudflare-WARP（打通-IPv4-出口）" class="headerlink" title="① 首先安装 Cloudflare WARP（打通 IPv4 出口）"></a>① 首先安装 Cloudflare WARP（打通 IPv4 出口）</h2><p>纯 IPv6 VPS <strong>只有 IPv6 出口</strong>：访问 IPv4 网站（GitHub、多数 CDN）会报 <code>network is unreachable</code>。而 Hysteria 2 代理出站的目标大多是 IPv4 网站，所以<strong>第一步先装 WARP</strong>，借 Cloudflare 的隧道获得 IPv4 出口，后续下载、更新、代理全都能跑通。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">wget -N https://gitlab.com/fscarmen/warp/-/raw/main/menu.sh &amp;&amp; bash menu.sh 4</span><br></pre></td></tr></table></figure><blockquote><p>⚠️ <strong>如果脚本下载 wireguard-go 失败</strong>：GitHub 不支持 IPv6，而脚本需要从 GitHub 拉二进制。先临时配置 NAT64 DNS 再执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> -e <span class="string">&quot;nameserver 2a01:4f8:c2c:123f::1\nnameserver 2001:67c:2b0::4&quot;</span> &gt; /etc/resolv.conf</span><br></pre></td></tr></table></figure></blockquote><p><strong>脚本参数速查</strong>（重点，别记错）：</p><table><thead><tr><th align="left">参数</th><th align="left">作用</th><th align="left">适用场景</th></tr></thead><tbody><tr><td align="left"><strong>4</strong></td><td align="left">添加 <strong>IPv4</strong> 出口</td><td align="left">✅ <strong>纯 IPv6 VPS 用这个</strong></td></tr><tr><td align="left">6</td><td align="left">添加 IPv6 出口</td><td align="left">纯 IPv4 VPS 用</td></tr><tr><td align="left">d</td><td align="left">双栈</td><td align="left">双栈 VPS 用</td></tr><tr><td align="left">u</td><td align="left">卸载 WARP</td><td align="left">装错 &#x2F; 想移除时</td></tr><tr><td align="left">s</td><td align="left">查看状态 &#x2F; 切换优先级</td><td align="left">检查用</td></tr></tbody></table><p>安装过程中的交互选项（WireGuard 方式、工作模式、账户）<strong>全部保持默认</strong>；<strong>优先级建议选 <code>IPv6</code> 优先</strong>——IPv6 资源走原生 IPv6，仅 IPv4 资源借 WARP 出口。</p><h3 id="WARP-常见边界"><a href="#WARP-常见边界" class="headerlink" title="WARP 常见边界"></a>WARP 常见边界</h3><p>WARP 只负责 VPS 的出站访问，不会替代 Hysteria 2 的监听端口，也不会让纯 IPv6 VPS 获得一个可被客户端直连的公网 IPv4。客户端仍然需要通过 AAAA 记录访问 VPS。若服务商已经提供原生 IPv4，不必为了“看起来完整”强行安装 WARP，但仍可按需保留它作为出站策略。</p><hr><h2 id="②-然后测试（安装后立即验证）"><a href="#②-然后测试（安装后立即验证）" class="headerlink" title="② 然后测试（安装后立即验证）"></a>② 然后测试（安装后立即验证）</h2><p><strong>服务端验证 IPv4 出口已生效：</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">curl -4 -s https://www.cloudflare.com/cdn-cgi/trace | grep -E <span class="string">&quot;ip=|warp=&quot;</span></span><br></pre></td></tr></table></figure><p>预期输出（<code>warp=on</code> 且 <code>ip=</code> 是一个 IPv4 地址）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ip=104.28.x.x</span><br><span class="line">warp=on</span><br></pre></td></tr></table></figure><p><strong>双向连通性测试：</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># IPv4 出站（借 WARP）—— 预期 200</span></span><br><span class="line">curl -4 -s -o /dev/null -w <span class="string">&quot;%&#123;http_code&#125;&quot;</span> https://www.google.com</span><br><span class="line"><span class="comment"># IPv6 出站（原生）—— 预期 200</span></span><br><span class="line">curl -6 -s -o /dev/null -w <span class="string">&quot;%&#123;http_code&#125;&quot;</span> https://www.google.com</span><br></pre></td></tr></table></figure><p><strong>本机（客户端）侧测试：</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 本机 IPv6 出口是否正常 —— 必须能通</span></span><br><span class="line">ping -6 -n 3 2606:4700:4700::1111</span><br><span class="line"><span class="comment"># 域名解析出的 IPv6 是否等于 VPS 当前 IP</span></span><br><span class="line">nslookup -<span class="built_in">type</span>=AAAA yourname.ddns.org</span><br></pre></td></tr></table></figure><blockquote><p>判断标准：本机 IPv6 不通 → 检查运营商 IPv6、路由器、VPN&#x2F;代理软件（如本机 WARP）是否干扰，先解决再继续。</p></blockquote><hr><h2 id="③-安装-Hysteria-2"><a href="#③-安装-Hysteria-2" class="headerlink" title="③ 安装 Hysteria 2"></a>③ 安装 Hysteria 2</h2><p>有了 WARP 的 IPv4 出口后，官方脚本一般可以直接跑通：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">apt update &amp;&amp; apt upgrade -y</span><br><span class="line">bash &lt;(curl -fsSL https://get.hy2.sh/)</span><br></pre></td></tr></table></figure><p><strong>如果仍下载失败</strong>（部分网络环境下 GitHub 访问受限），二选一：</p><ul><li><strong>方案 A</strong>：临时 NAT64 DNS 后重试（同 WARP 那步）<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> -e <span class="string">&quot;nameserver 2a01:4f8:c2c:123f::1\nnameserver 2001:67c:2b0::4&quot;</span> &gt; /etc/resolv.conf</span><br><span class="line">bash &lt;(curl -fsSL https://get.hy2.sh/)</span><br></pre></td></tr></table></figure></li><li><strong>方案 B</strong>：在能上网的机器下载 <code>hysteria-linux-amd64</code>（<a href="https://github.com/apernet/hysteria/releases">GitHub Releases</a>），上传到 VPS 后本地安装：<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">bash &lt;(curl -fsSL https://get.hy2.sh/) --<span class="built_in">local</span> /home/hysteria-linux-amd64</span><br></pre></td></tr></table></figure></li></ul><p>安装完成后会生成系统服务 <code>hysteria-server</code>，默认监听 UDP 8443。</p><p>安装后先确认二进制和服务是否存在：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">hysteria version</span><br><span class="line">systemctl <span class="built_in">cat</span> hysteria-server --no-pager | sed -n <span class="string">&#x27;1,120p&#x27;</span></span><br></pre></td></tr></table></figure><p>若 <code>hysteria</code> 不在 PATH 中，先用 <code>systemctl cat</code> 查看服务实际使用的二进制路径；不要随意从第三方下载同名文件覆盖官方安装结果。</p><hr><h2 id="④-配置-Hysteria-2"><a href="#④-配置-Hysteria-2" class="headerlink" title="④ 配置 Hysteria 2"></a>④ 配置 Hysteria 2</h2><p>写入配置文件（示例值请替换）：</p><p><span style="color:#d14;font-weight:700">需要修改：<code>password</code>。如果你使用自己的端口，还要同步修改 <code>listen</code>。</span></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> &lt;&lt; <span class="string">&#x27;EOF&#x27;</span> &gt; /etc/hysteria/config.yaml</span><br><span class="line">listen: :8443</span><br><span class="line"></span><br><span class="line">tls:</span><br><span class="line">  cert: /etc/hysteria/server.crt</span><br><span class="line">  key: /etc/hysteria/server.key</span><br><span class="line"></span><br><span class="line">auth:</span><br><span class="line">  <span class="built_in">type</span>: password</span><br><span class="line">  password: YourStrongPassword!2026    <span class="comment"># ← 换成自己的强密码</span></span><br><span class="line">EOF</span><br></pre></td></tr></table></figure><p>写入后检查 YAML 是否包含预期字段，并确认密码没有被 shell 展开或截断：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sed -n <span class="string">&#x27;1,120p&#x27;</span> /etc/hysteria/config.yaml</span><br></pre></td></tr></table></figure><p>密码不要包含未处理的空格、换行或 YAML 特殊结构。最稳妥的做法是使用随机生成的字母、数字和少量常见符号，并在客户端逐字复制。</p><blockquote><p><code>listen: :8443</code> 表示监听 UDP 8443（Hysteria 2 基于 UDP&#x2F;QUIC）。端口可自定义，但需与客户端、防火墙保持一致。</p></blockquote><hr><h2 id="⑤-证书（生成带-SAN-的自签证书）"><a href="#⑤-证书（生成带-SAN-的自签证书）" class="headerlink" title="⑤ 证书（生成带 SAN 的自签证书）"></a>⑤ 证书（生成带 SAN 的自签证书）</h2><h3 id="5-1-为什么必须带-SAN"><a href="#5-1-为什么必须带-SAN" class="headerlink" title="5.1 为什么必须带 SAN"></a>5.1 为什么必须带 SAN</h3><blockquote><p>🔴 <strong>最大的坑</strong>：Go 1.15+（sing-box &#x2F; Xray 均为 Go 编写）校验 TLS <strong>只认 subjectAltName（SAN），不认 CN</strong>。<br>只写 <code>-subj &quot;/CN=域名&quot;</code> 的证书即使被客户端信任，也会被直接拒绝连接。</p></blockquote><h3 id="5-2-生成证书"><a href="#5-2-生成证书" class="headerlink" title="5.2 生成证书"></a>5.2 生成证书</h3><p>DNS 与 IP 必须写在<strong>同一条</strong> <code>-addext</code> 里（逗号分隔）——写成两条会报 <code>Duplicate extension</code> 导致生成失败：</p><p><span style="color:#d14;font-weight:700">需要修改：<code>-subj</code> 中的域名，以及 <code>subjectAltName</code> 中的 DNS 和 IPv6。DNS 必须与客户端 SNI、连接域名一致。</span></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">openssl req -x509 -newkey ec -pkeyopt ec_paramgen_curve:prime256v1 -nodes \</span><br><span class="line">  -keyout /etc/hysteria/server.key -out /etc/hysteria/server.crt -days 3650 \</span><br><span class="line">  -subj <span class="string">&quot;/CN=yourname.ddns.org&quot;</span> \</span><br><span class="line">  -addext <span class="string">&quot;subjectAltName=DNS:yourname.ddns.org,IP:2001:db8:abcd::1&quot;</span></span><br></pre></td></tr></table></figure><p>验证 SAN 已写入（必须能看到 <code>DNS:yourname.ddns.org, IP:2001:db8:abcd::1</code>）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl x509 -<span class="keyword">in</span> /etc/hysteria/server.crt -noout -subject -dates -ext subjectAltName</span><br></pre></td></tr></table></figure><h3 id="5-3-修正文件权限（关键，最容易漏）"><a href="#5-3-修正文件权限（关键，最容易漏）" class="headerlink" title="5.3 修正文件权限（关键，最容易漏）"></a>5.3 修正文件权限（关键，最容易漏）</h3><blockquote><p>🔴 官方脚本创建的服务以 <strong><code>hysteria</code> 用户</strong>运行（非 root）。<code>openssl</code> 生成的文件属主是 root，<strong>服务会报 <code>permission denied</code> 起不来</strong>。</p></blockquote><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chown</span> hysteria:hysteria /etc/hysteria/server.key /etc/hysteria/server.crt</span><br><span class="line"><span class="built_in">chmod</span> 600 /etc/hysteria/server.key     <span class="comment"># 私钥仅属主可读写</span></span><br><span class="line"><span class="built_in">chmod</span> 644 /etc/hysteria/server.crt     <span class="comment"># 公钥证书可读</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 确认属主（预期 server.key/crt 的属主都是 hysteria）</span></span><br><span class="line"><span class="built_in">ls</span> -l /etc/hysteria/</span><br></pre></td></tr></table></figure><h3 id="5-4-防火墙放行-启动"><a href="#5-4-防火墙放行-启动" class="headerlink" title="5.4 防火墙放行 + 启动"></a>5.4 防火墙放行 + 启动</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">ufw allow 8443/udp</span><br><span class="line">ufw allow 8443/tcp</span><br><span class="line">ufw <span class="built_in">enable</span>        <span class="comment"># 若提示未安装则跳过</span></span><br><span class="line">systemctl <span class="built_in">enable</span> --now hysteria-server</span><br><span class="line">systemctl status hysteria-server</span><br></pre></td></tr></table></figure><p>预期看到 <code>active (running)</code>，日志含 <code>server up and running {&quot;listen&quot;: &quot;:8443&quot;}</code>。</p><blockquote><p>若启动失败，用 <code>journalctl -u hysteria-server -n 30 --no-pager</code> 看报错，常见两类：</p><ul><li><code>tls.key: permission denied</code> → 回到 5.3 修正属主</li><li><code>tls.cert: no such file or directory</code> → 证书没生成成功（检查 5.2 的 openssl 是否报错）</li></ul></blockquote><h3 id="5-5-从外部确认-UDP-端口"><a href="#5-5-从外部确认-UDP-端口" class="headerlink" title="5.5 从外部确认 UDP 端口"></a>5.5 从外部确认 UDP 端口</h3><p>服务器本地 <code>ss</code> 只能证明进程正在监听，不能证明云平台安全组或上游防火墙已经放行。请同时检查：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ss -lunp | grep <span class="string">&#x27;:8443&#x27;</span></span><br><span class="line">ufw status verbose</span><br></pre></td></tr></table></figure><p>如果 VPS 服务商有独立的安全组&#x2F;网络 ACL，还要在控制台新增 <strong>UDP 8443 入站规则</strong>。TCP 8443 对 Hysteria 2 本身不是必需的；不需要时可以关闭，以减少暴露面。</p><hr><h2 id="⑥-导入客户端-配置证书"><a href="#⑥-导入客户端-配置证书" class="headerlink" title="⑥ 导入客户端 &amp; 配置证书"></a>⑥ 导入客户端 &amp; 配置证书</h2><h3 id="6-1-一键导入链接（先用起来）"><a href="#6-1-一键导入链接（先用起来）" class="headerlink" title="6.1 一键导入链接（先用起来）"></a>6.1 一键导入链接（先用起来）</h3><p>复制后到 v2rayN &#x2F; NekoBox 主界面按 <strong><code>Ctrl + V</code></strong> 导入：</p><p><span style="color:#d14;font-weight:700">需要修改：链接中的密码、域名和端口。链接含密码，分享前必须删除或重新生成。</span></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hy2://YourStrongPassword!2026@yourname.ddns.org:8443/?insecure=1&amp;sni=yourname.ddns.org#IPv6-Hysteria2</span><br></pre></td></tr></table></figure><blockquote><p><code>insecure=1</code> 表示跳过证书验证——<strong>能用</strong>，但客户端会提示不安全配置。想要零警告，用下面的固定证书方案。</p></blockquote><h3 id="6-2-手动配置（推荐：固定证书方案，零警告）"><a href="#6-2-手动配置（推荐：固定证书方案，零警告）" class="headerlink" title="6.2 手动配置（推荐：固定证书方案，零警告）"></a>6.2 手动配置（推荐：固定证书方案，零警告）</h3><p><strong>原理</strong>：自签证书可以做到”既不用跳过验证、又没警告”——把服务器证书<strong>固定</strong>到客户端。</p><p><span style="color:#16803c;font-weight:700">推荐顺序：先用 <code>insecure=1</code> 验证网络和密码，再切换为固定证书并关闭“跳过证书验证”。这样能把网络故障与证书信任故障分开定位。</span></p><p>先在服务器上查看证书，复制<strong>完整多行文本</strong>（<code>-----BEGIN CERTIFICATE-----</code> 到 <code>-----END CERTIFICATE-----</code>）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> /etc/hysteria/server.crt</span><br></pre></td></tr></table></figure><p>然后 v2rayN 节点编辑（<strong>sing-box 核心</strong>）：</p><table><thead><tr><th align="left">字段</th><th align="left">填写</th><th align="left">备注</th></tr></thead><tbody><tr><td align="left">协议类型</td><td align="left"><code>Hysteria 2</code></td><td align="left">—</td></tr><tr><td align="left">核心</td><td align="left"><code>sing_box</code></td><td align="left">⚠️ 别用 Xray 跑这个节点</td></tr><tr><td align="left">服务器地址</td><td align="left"><span style="color:#d14;font-weight:700"><code>yourname.ddns.org</code></span></td><td align="left">或 <span style="color:#d14"><code>[2001:db8:abcd::1]</code></span></td></tr><tr><td align="left">端口</td><td align="left"><code>8443</code></td><td align="left">与服务端一致</td></tr><tr><td align="left">认证密码</td><td align="left"><span style="color:#d14;font-weight:700"><code>YourStrongPassword!2026</code></span></td><td align="left">与服务端一致</td></tr><tr><td align="left">SNI</td><td align="left"><span style="color:#d14;font-weight:700"><code>yourname.ddns.org</code></span></td><td align="left"><strong>必须与证书 SAN 一致</strong></td></tr><tr><td align="left"><strong>跳过证书验证</strong></td><td align="left"><strong>不勾选</strong></td><td align="left">固定证书后可以放心关闭</td></tr><tr><td align="left"><strong>固定证书</strong></td><td align="left">粘贴完整 PEM 多行文本</td><td align="left"><strong>不是公钥哈希</strong>（填哈希会报错）</td></tr></tbody></table><blockquote><p>🔴 <strong>固定证书的坑</strong>：</p><ul><li>填的是<strong>证书文本（PEM）</strong>，<strong>不是</strong> <code>openssl</code> 算出来的公钥哈希。把哈希填进 Xray 核心会直接启动失败：<code>encoding/hex: invalid byte: &#39;U&#39;</code>。</li><li>证书与密钥是一对，<strong>服务器换过证书后，客户端必须同步换</strong>，否则连不上（见第 ⑦ 节）。</li></ul></blockquote><p>完成客户端配置后，按以下顺序验证：</p><ol><li>先确认节点地址解析到当前 VPS：<code>nslookup -type=AAAA yourname.ddns.org</code>。</li><li>在客户端仅开启该节点，访问一个 IPv4-only 站点和一个 IPv6 站点。</li><li>确认服务端日志没有持续的 <code>authentication failed</code>、<code>tls</code> 或 <code>network is unreachable</code>。</li><li>固定证书模式稳定运行后，删除临时的 <code>insecure=1</code> 链接，避免误用不安全配置。</li></ol><h3 id="6-3-核心选择"><a href="#6-3-核心选择" class="headerlink" title="6.3 核心选择"></a>6.3 核心选择</h3><table><thead><tr><th align="left">核心</th><th align="left">结论</th></tr></thead><tbody><tr><td align="left"><strong>sing-box</strong></td><td align="left">✅ 推荐。固定证书（PEM）与 insecure 均正常</td></tr><tr><td align="left">Xray</td><td align="left">❌ 实测 Hysteria2 的证书相关字段映射有 bug（hex 解析错误 &#x2F; 无法信任自签证书）</td></tr></tbody></table><hr><h2 id="⑦-注意事项-更新配置说明"><a href="#⑦-注意事项-更新配置说明" class="headerlink" title="⑦ 注意事项 &amp; 更新配置说明"></a>⑦ 注意事项 &amp; 更新配置说明</h2><h3 id="7-1-常见故障速查"><a href="#7-1-常见故障速查" class="headerlink" title="7.1 常见故障速查"></a>7.1 常见故障速查</h3><table><thead><tr><th align="left">现象</th><th align="left">原因</th><th align="left">解决</th></tr></thead><tbody><tr><td align="left">测延迟总是 <code>-1</code></td><td align="left">本机 IPv6 不通 &#x2F; 域名解析错误 &#x2F; 橙云代理</td><td align="left">依次排查：本机 <code>ping -6</code>、<code>nslookup -type=AAAA</code>、Cloudflare 改灰云</td></tr><tr><td align="left">能连上但网页打不开，日志报 <code>dial tcp4: network is unreachable</code></td><td align="left">VPS 缺 IPv4 出口</td><td align="left">重跑 <code>bash menu.sh 4</code>，验证 <code>curl -4 ... | grep warp=</code> 为 <code>on</code></td></tr><tr><td align="left">客户端提示 <code>AllowInsecure</code> &#x2F; 报 <code>x509: certificate signed by unknown authority</code></td><td align="left">未固定证书，或证书 SAN 缺域名</td><td align="left">按 6.2 固定证书；检查 SAN</td></tr><tr><td align="left">服务启动失败 <code>permission denied</code>（tls.key）</td><td align="left">证书属主不是 hysteria</td><td align="left"><code>chown hysteria:hysteria</code> + <code>chmod 600/644</code> + 重启</td></tr><tr><td align="left">客户端启动失败 <code>encoding/hex: invalid byte</code></td><td align="left">固定证书填成了哈希</td><td align="left">清空改粘完整 PEM 文本</td></tr><tr><td align="left">本机 IPv6 突然不通 &#x2F; DNS 异常</td><td align="left">本机开着 Cloudflare WARP 或其它 VPN</td><td align="left">使用 v2rayN 期间建议<strong>断开本机 WARP</strong>；交叉验证 <code>nslookup 域名 223.5.5.5</code></td></tr><tr><td align="left">服务器全部端口不通（掉线）</td><td align="left">服务器端问题 &#x2F; IP 变更 &#x2F; 免费机被回收</td><td align="left">登录服务商控制台查实例状态与当前 IP，必要时重启</td></tr></tbody></table><h3 id="7-2-更新-Hysteria-2"><a href="#7-2-更新-Hysteria-2" class="headerlink" title="7.2 更新 Hysteria 2"></a>7.2 更新 Hysteria 2</h3><p>官方脚本支持重复执行，重跑即自动升级二进制并重启服务：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">bash &lt;(curl -fsSL https://get.hy2.sh/)</span><br><span class="line">hysteria version          <span class="comment"># 确认版本</span></span><br><span class="line">systemctl status hysteria-server   <span class="comment"># 确认 active (running)</span></span><br></pre></td></tr></table></figure><blockquote><p>当初用 <code>--local</code> 安装的，升级时同样加 <code>--local /home/新二进制路径</code>。</p></blockquote><h3 id="7-3-更新配置时的同步规则"><a href="#7-3-更新配置时的同步规则" class="headerlink" title="7.3 更新配置时的同步规则"></a>7.3 更新配置时的同步规则</h3><p>任何服务端变更，记得<strong>客户端要同步改</strong>：</p><table><thead><tr><th align="left">服务端改动</th><th align="left">操作</th><th align="left">客户端需同步</th></tr></thead><tbody><tr><td align="left"><strong>换证书</strong></td><td align="left">重新生成 cert&#x2F;key → chown&#x2F;chmod → 重启服务</td><td align="left"><strong>固定证书必须换新 PEM</strong>，否则 <code>unknown authority</code></td></tr><tr><td align="left"><strong>改密码</strong></td><td align="left">改 <code>config.yaml</code> 的 <code>auth.password</code> → 重启</td><td align="left">更新认证密码</td></tr><tr><td align="left"><strong>改端口</strong></td><td align="left">改 <code>listen:</code> + <code>ufw allow 新端口</code> → 重启</td><td align="left">更新端口</td></tr></tbody></table><h3 id="7-4-推荐加固：SSH-换端口（Ubuntu-24-04-必看）"><a href="#7-4-推荐加固：SSH-换端口（Ubuntu-24-04-必看）" class="headerlink" title="7.4 推荐加固：SSH 换端口（Ubuntu 24.04 必看）"></a>7.4 推荐加固：SSH 换端口（Ubuntu 24.04 必看）</h3><blockquote><p>⚠️ <strong>Ubuntu 24.04 的坑</strong>：默认启用 <code>ssh.socket</code>（套接字激活），监听端口由 systemd 持有——<code>sshd_config</code> 里的 <code>Port</code> 会被它无视。改完端口连不上时，先查 <code>systemctl status ssh</code> 是否显示 <code>TriggeredBy: ● ssh.socket</code>。</p></blockquote><p>改端口 22 → 2222 的正确姿势：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">systemctl <span class="built_in">disable</span> --now ssh.socket   <span class="comment"># 关掉 socket 激活</span></span><br><span class="line">systemctl restart ssh                <span class="comment"># sshd 直接监听配置端口</span></span><br><span class="line">ss -tlnp | grep ssh                  <span class="comment"># 确认只剩 0.0.0.0:2222 / [::]:2222</span></span><br></pre></td></tr></table></figure><p><strong>安全流程</strong>：新开一个终端 <code>ssh -p 2222 root@yourname.ddns.org</code> 登录成功后再关旧会话；失败则回滚 <code>systemctl enable --now ssh.socket &amp;&amp; systemctl restart ssh</code>。</p><h3 id="7-5-免费-VPS-日常维护"><a href="#7-5-免费-VPS-日常维护" class="headerlink" title="7.5 免费 VPS 日常维护"></a>7.5 免费 VPS 日常维护</h3><table><thead><tr><th align="left">事项</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left"><strong>保活</strong></td><td align="left">EUserv 等免费实例<strong>定期登录控制台</strong>（建议每 3~4 周），防止被回收</td></tr><tr><td align="left"><strong>IP 变更</strong></td><td align="left">掉线先查控制台当前 IP；变更后同步更新 DDNS 与客户端节点</td></tr><tr><td align="left"><strong>证书有效期</strong></td><td align="left">本方案证书 10 年（<code>-days 3650</code>），到期前重新生成并同步客户端</td></tr><tr><td align="left"><strong>密码强度</strong></td><td align="left"><code>auth.password</code> 用高强度随机密码，勿用教程示例值</td></tr><tr><td align="left"><strong>负载虚高</strong></td><td align="left">容器内 CPU 100% idle 但 loadavg 3+ 是 LXD 宿主负载，超售常态，忽略即可，不影响性能</td></tr></tbody></table><h3 id="7-6-配置变更的安全流程"><a href="#7-6-配置变更的安全流程" class="headerlink" title="7.6 配置变更的安全流程"></a>7.6 配置变更的安全流程</h3><p>修改配置时，建议始终遵循“备份 → 检查 → 重启 → 查看日志 → 客户端同步”的顺序：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cp</span> -a /etc/hysteria/config.yaml /etc/hysteria/config.yaml.bak.$(<span class="built_in">date</span> +%F-%H%M%S)</span><br><span class="line"><span class="comment"># 使用编辑器修改后，先检查文件内容</span></span><br><span class="line">sed -n <span class="string">&#x27;1,120p&#x27;</span> /etc/hysteria/config.yaml</span><br><span class="line">systemctl restart hysteria-server</span><br><span class="line">systemctl is-active --quiet hysteria-server &amp;&amp; <span class="built_in">echo</span> <span class="string">&#x27;service is active&#x27;</span></span><br><span class="line">journalctl -u hysteria-server -n 30 --no-pager</span><br></pre></td></tr></table></figure><p>如果重启后服务不活跃，先恢复最近一次备份再重启：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cp</span> -a /etc/hysteria/config.yaml.bak.YYYY-MM-DD-HHMMSS /etc/hysteria/config.yaml</span><br><span class="line">systemctl restart hysteria-server</span><br></pre></td></tr></table></figure><p><span style="color:#d14;font-weight:700">上面的 <code>YYYY-MM-DD-HHMMSS</code> 只是脱敏示例，请替换成实际备份文件名；不要原样执行。</span></p><h3 id="7-7-资源与日志管理"><a href="#7-7-资源与日志管理" class="headerlink" title="7.7 资源与日志管理"></a>7.7 资源与日志管理</h3><p>纯 IPv6 免费 VPS 的内存和磁盘通常比较紧张，定期关注资源使用情况：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">free -h</span><br><span class="line"><span class="built_in">df</span> -h /</span><br><span class="line">journalctl --disk-usage</span><br></pre></td></tr></table></figure><p>不要为了“清理日志”直接删除 <code>/var/log</code> 或 systemd 日志目录。应使用 <code>journalctl --vacuum-time=30d</code> 按时间清理，并先确认没有正在进行的故障排查。</p><hr><h2 id="附：命令速查表"><a href="#附：命令速查表" class="headerlink" title="附：命令速查表"></a>附：命令速查表</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># ── ① WARP：加 IPv4 出口（纯 IPv6 VPS）────────────</span></span><br><span class="line">wget -N https://gitlab.com/fscarmen/warp/-/raw/main/menu.sh &amp;&amp; bash menu.sh 4</span><br><span class="line">curl -4 -s https://www.cloudflare.com/cdn-cgi/trace | grep -E <span class="string">&quot;ip=|warp=&quot;</span>   <span class="comment"># 预期 warp=on</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># ── ② 安装 Hysteria 2 ─────────────────────────────</span></span><br><span class="line">bash &lt;(curl -fsSL https://get.hy2.sh/)</span><br><span class="line"></span><br><span class="line"><span class="comment"># ── ③ 配置 + 证书 + 权限 + 启动 ──────────────────</span></span><br><span class="line"><span class="built_in">cat</span> &gt; /etc/hysteria/config.yaml &lt;&lt;<span class="string">&#x27;EOF&#x27;</span></span><br><span class="line">listen: :8443</span><br><span class="line">tls:</span><br><span class="line">  cert: /etc/hysteria/server.crt</span><br><span class="line">  key: /etc/hysteria/server.key</span><br><span class="line">auth:</span><br><span class="line">  <span class="built_in">type</span>: password</span><br><span class="line">  password: YourStrongPassword!2026</span><br><span class="line">EOF</span><br><span class="line"></span><br><span class="line">openssl req -x509 -newkey ec -pkeyopt ec_paramgen_curve:prime256v1 -nodes \</span><br><span class="line">  -keyout /etc/hysteria/server.key -out /etc/hysteria/server.crt -days 3650 \</span><br><span class="line">  -subj <span class="string">&quot;/CN=yourname.ddns.org&quot;</span> \</span><br><span class="line">  -addext <span class="string">&quot;subjectAltName=DNS:yourname.ddns.org,IP:2001:db8:abcd::1&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">chown</span> hysteria:hysteria /etc/hysteria/server.key /etc/hysteria/server.crt</span><br><span class="line"><span class="built_in">chmod</span> 600 /etc/hysteria/server.key &amp;&amp; <span class="built_in">chmod</span> 644 /etc/hysteria/server.crt</span><br><span class="line"></span><br><span class="line">ufw allow 8443/udp &amp;&amp; ufw allow 8443/tcp</span><br><span class="line">systemctl <span class="built_in">enable</span> --now hysteria-server</span><br><span class="line"></span><br><span class="line"><span class="comment"># ── 客户端（Windows 自测）─────────────────────────</span></span><br><span class="line">ping -6 -n 3 2606:4700:4700::1111      <span class="comment"># 本机 IPv6 出口</span></span><br><span class="line">nslookup -<span class="built_in">type</span>=AAAA yourname.ddns.org  <span class="comment"># 域名解析核对</span></span><br></pre></td></tr></table></figure><hr><p><em>本文基于真实部署与排障过程整理：WARP 前置打通 IPv4 出口、证书 SAN（单条 -addext 写 DNS+IP）、hysteria 用户权限、sing-box 固定证书（PEM 而非哈希）、Ubuntu 24.04 ssh.socket 改端口等均为实测验证过的关键点。</em></p>]]>
    </content>
    <id>https://web-xmind.204680.xyz/2026/08/27/Hysteria2-%E7%BA%AFIPv6-VPS-%E6%90%AD%E5%BB%BA%E6%95%99%E7%A8%8B/</id>
    <link href="https://web-xmind.204680.xyz/2026/08/27/Hysteria2-%E7%BA%AFIPv6-VPS-%E6%90%AD%E5%BB%BA%E6%95%99%E7%A8%8B/"/>
    <published>2026-08-27T09:06:00.000Z</published>
    <summary>面向纯 IPv6 VPS 的 Hysteria 2 搭建全流程：先装 Cloudflare WARP 打通 IPv4 出口，再安装 Hysteria 2、生成带 SAN 的自签证书、客户端固定证书零警告接入，附隐私脱敏示例值。</summary>
    <title>纯 IPv6 VPS 搭建 Hysteria 2 节点：从零到稳定运行的完整教程</title>
    <updated>2026-08-28T13:25:34.202Z</updated>
  </entry>
</feed>
